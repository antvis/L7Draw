export enum DrawerEvent {
  init = 'init',
  destroy = 'destroy',
  enable = 'enable',
  disable = 'disable',
  add = 'add',
  dragging = 'dragging',
  dragEnd = 'dragEnd',
  change = 'change',
  click = 'click',
}

export enum SourceEvent {
  change = 'change',
}

export enum RenderEvent {}
